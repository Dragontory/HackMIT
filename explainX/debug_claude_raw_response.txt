{
    "class_name": "GradientDescentExplanation",
    "description": "A comprehensive educational scene explaining gradient descent, its mathematical foundations, and practical applications in optimization problems.",
    "estimated_duration": 240,
    "key_concepts": [
        "Gradient Descent Fundamentals",
        "Mathematical Formulation",
        "Optimization Process",
        "Convergence Properties",
        "Batch vs. Stochastic Gradient Descent",
        "Visualization of Descent Process"
    ],
    "python_code": "from manim import *\nimport numpy as np\n\nclass GradientDescentExplanation(Scene):\n    def construct(self):\n        # Introduction\n        title = Text(\"Gradient Descent\", font_size=48)\n        subtitle = Text(\"A Practical Guide to Optimization\", font_size=32)\n        subtitle.next_to(title, DOWN)\n        \n        self.play(Write(title))\n        self.play(FadeIn(subtitle))\n        self.wait(2)\n        self.play(FadeOut(title), FadeOut(subtitle))\n        \n        # Section 1: What is Gradient Descent?\n        section_title = Text(\"What is Gradient Descent?\", font_size=40)\n        self.play(Write(section_title))\n        self.wait(1)\n        self.play(section_title.animate.to_edge(UP))\n        \n        # Basic explanation\n        explanation = Text(\n            \"Gradient descent is an optimization algorithm\\n\"\n            \"used to minimize a function by iteratively\\n\"\n            \"moving in the direction of steepest descent.\",\n            font_size=24,\n            line_spacing=1.5\n        )\n        explanation.next_to(section_title, DOWN, buff=0.5)\n        \n        self.play(Write(explanation), run_time=2)\n        self.wait(2)\n        self.play(FadeOut(explanation))\n        \n        # Mathematical formulation\n        objective_function = MathTex(r\"f(\\theta) \\text{ where } \\theta \\in \\mathbb{R}^d\")\n        objective_function.next_to(section_title, DOWN, buff=0.5)\n        self.play(Write(objective_function))\n        self.wait(1)\n        \n        gradient_def = MathTex(r\"\\nabla f(\\theta) = \\text{direction of steepest increase}\")\n        gradient_def.next_to(objective_function, DOWN, buff=0.5)\n        self.play(Write(gradient_def))\n        self.wait(1)\n        \n        update_rule = MathTex(r\"\\theta_{t+1} = \\theta_t - \\alpha \\nabla f(\\theta_t)\")\n        update_rule.next_to(gradient_def, DOWN, buff=0.5)\n        self.play(Write(update_rule))\n        \n        alpha_explanation = Text(\"α is the learning rate or step size\", font_size=24)\n        alpha_explanation.next_to(update_rule, DOWN, buff=0.3)\n        self.play(Write(alpha_explanation))\n        self.wait(2)\n        \n        # Group all math elements\n        math_group = VGroup(objective_function, gradient_def, update_rule, alpha_explanation)\n        \n        # Visualization of gradient descent in 2D\n        self.play(FadeOut(math_group))\n        \n        # Create a 3D surface for visualization\n        axes = Axes(\n            x_range=[-3, 3, 1],\n            y_range=[-1, 5, 1],\n            axis_config={\"include_tip\": False},\n        )\n        \n        # Define a simple bowl function f(x) = x^2\n        def func(x):\n            return x**2\n        \n        graph = axes.plot(func, color=BLUE)\n        \n        # Add labels\n        x_label = axes.get_x_axis_label(\"\\\\theta\")\n        y_label = axes.get_y_axis_label(\"f(\\\\theta)\")\n        \n        graph_group = VGroup(axes, graph, x_label, y_label)\n        graph_group.scale(0.8).to_edge(DOWN, buff=0.5)\n        \n        self.play(Create(axes), Create(graph), Write(x_label), Write(y_label))\n        \n        # Show gradient descent steps on the graph\n        dot_start = Dot(axes.c2p(-2.5, func(-2.5)), color=RED)\n        self.play(FadeIn(dot_start))\n        \n        # Perform gradient descent steps\n        current_x = -2.5\n        learning_rate = 0.3\n        steps = 5\n        \n        dots = [dot_start]\n        arrows = []\n        \n        for i in range(steps):\n            # Compute gradient: df/dx = 2x for f(x) = x^2\n            gradient = 2 * current_x\n            \n            # Update x\n            new_x = current_x - learning_rate * gradient\n            \n            # Create new dot\n            new_dot = Dot(axes.c2p(new_x, func(new_x)), color=RED)\n            dots.append(new_dot)\n            \n            # Create arrow\n            arrow = Arrow(\n                start=axes.c2p(current_x, func(current_x)),\n                end=axes.c2p(new_x, func(new_x)),\n                buff=0,\n                color=YELLOW,\n                max_tip_length_to_length_ratio=0.15,\n                max_stroke_width_to_length_ratio=3\n            )\n            arrows.append(arrow)\n            \n            # Update current_x\n            current_x = new_x\n        \n        # Animate the steps\n        for i in range(steps):\n            self.play(\n                GrowArrow(arrows[i]),\n                TransformFromCopy(dots[i], dots[i+1]),\n                run_time=1\n            )\n            step_text = Text(f\"Step {i+1}\", font_size=20).to_corner(UR)\n            self.play(Write(step_text))\n            self.wait(0.5)\n            self.play(FadeOut(step_text))\n        \n        self.wait(1)\n        \n        # Clean up\n        self.play(\n            FadeOut(section_title),\n            *[FadeOut(dot) for dot in dots],\n            *[FadeOut(arrow) for arrow in arrows],\n            FadeOut(graph_group)\n        )\n        \n        # Section 2: Convergence Properties\n        section_title = Text(\"Convergence Properties\", font_size=40)\n        self.play(Write(section_title))\n        self.wait(1)\n        self.play(section_title.animate.to_edge(UP))\n        \n        # Explain convergence for convex functions\n        convex_text = Text(\"For convex, L-smooth functions:\", font_size=28)\n        convex_text.next_to(section_title, DOWN, buff=0.5)\n        self.play(Write(convex_text))\n        \n        # Convergence rate for convex functions\n        convex_rate = MathTex(r\"f(\\theta_t) - f(\\theta^*) = O\\left(\\frac{1}{t}\\right)\")\n        convex_rate.next_to(convex_text, DOWN, buff=0.5)\n        self.play(Write(convex_rate))\n        self.wait(1.5)\n        \n        # Explain convergence for strongly convex functions\n        strongly_convex_text = Text(\"For μ-strongly convex, L-smooth functions:\", font_size=28)\n        strongly_convex_text.next_to(convex_rate, DOWN, buff=0.5)\n        self.play(Write(strongly_convex_text))\n        \n        # Convergence rate for strongly convex functions\n        strongly_convex_rate = MathTex(r\"f(\\theta_t) - f(\\theta^*) \\leq (1-\\alpha\\mu)^t(f(\\theta_0) - f(\\theta^*))\")\n        strongly_convex_rate.next_to(strongly_convex_text, DOWN, buff=0.5)\n        self.play(Write(strongly_convex_rate))\n        self.wait(2)\n        \n        # Group all convergence elements\n        convergence_group = VGroup(convex_text, convex_rate, strongly_convex_text, strongly_convex_rate)\n        self.play(FadeOut(convergence_group))\n        \n        # Section 3: Batch vs. Stochastic Gradient Descent\n        section_title_new = Text(\"Batch vs. Stochastic Gradient Descent\", font_size=40)\n        self.play(ReplacementTransform(section_title, section_title_new))\n        self.wait(1)\n        \n        # Explain batch gradient descent\n        batch_title = Text(\"Batch Gradient Descent\", font_size=32)\n        batch_title.next_to(section_title_new, DOWN, buff=0.5)\n        self.play(Write(batch_title))\n        \n        batch_formula = MathTex(r\"\\nabla f(\\theta) = \\frac{1}{n}\\sum_{i=1}^{n}\\nabla \\ell(\\theta; x_i)\")\n        batch_formula.next_to(batch_title, DOWN, buff=0.3)\n        self.play(Write(batch_formula))\n        \n        batch_pros_cons = BulletedList(\n            \"Uses all training examples\",\n            \"Stable convergence\",\n            \"Computationally expensive\",\n            font_size=24\n        )\n        batch_pros_cons.next_to(batch_formula, DOWN, buff=0.3)\n        self.play(Write(batch_pros_cons))\n        self.wait(2)\n        \n        # Explain stochastic gradient descent\n        sgd_title = Text(\"Stochastic Gradient Descent (SGD)\", font_size=32)\n        sgd_title.next_to(section_title_new, DOWN, buff=0.5)\n        \n        sgd_formula = MathTex(r\"\\nabla f(\\theta) \\approx \\nabla \\ell(\\theta; x_i) \\text{ for random } i\")\n        sgd_formula.next_to(sgd_title, DOWN, buff=0.3)\n        \n        sgd_pros_cons = BulletedList(\n            \"Uses one example at a time\",\n            \"Faster iterations\",\n            \"Noisy updates\",\n            \"Mini-batch variant balances speed and stability\",\n            font_size=24\n        )\n        sgd_pros_cons.next_to(sgd_formula, DOWN, buff=0.3)\n        \n        self.play(\n            ReplacementTransform(batch_title, sgd_title),\n            ReplacementTransform(batch_formula, sgd_formula),\n            ReplacementTransform(batch_pros_cons, sgd_pros_cons)\n        )\n        self.wait(2)\n        \n        # Clean up\n        self.play(\n            FadeOut(section_title_new),\n            FadeOut(sgd_title),\n            FadeOut(sgd_formula),\n            FadeOut(sgd_pros_cons)\n        )\n        \n        # Conclusion\n        conclusion_title = Text(\"Key Takeaways\", font_size=40)\n        self.play(Write(conclusion_title))\n        self.wait(1)\n        self.play(conclusion_title.animate.to_edge(UP))\n        \n        takeaways = BulletedList(\n            \"Gradient descent minimizes functions by following negative gradients\",\n            \"Learning rate controls step size and affects convergence\",\n            \"Convergence guarantees exist for convex functions\",\n            \"Stochastic variants trade accuracy for computational efficiency\",\n            \"Forms the foundation of modern machine learning optimization\",\n            font_size=28,\n            line_spacing=1\n        )\n        takeaways.next_to(conclusion_title, DOWN, buff=0.5)\n        \n        self.play(Write(takeaways), run_time=3)\n        self.wait(3)\n        \n        # Final fade out\n        self.play(FadeOut(conclusion_title), FadeOut(takeaways))\n        \n        # End title\n        end_title = Text(\"Gradient Descent\", font_size=48)\n        end_subtitle = Text(\"The Foundation of Optimization\", font_size=32)\n        end_subtitle.next_to(end_title, DOWN)\n        \n        self.play(Write(end_title), Write(end_subtitle))\n        self.wait(2)\n        self.play(FadeOut(end_title), FadeOut(end_subtitle))",
    "notes": "This animation provides a comprehensive introduction to gradient descent, covering the mathematical foundations, convergence properties, and practical variants. The scene includes visualizations of the descent process on a simple quadratic function, mathematical formulations with proper notation, and comparisons between batch and stochastic gradient descent. The animation is structured to build understanding progressively, starting with basic concepts and moving to more advanced topics. The estimated duration is about 4 minutes, but this can be adjusted by modifying the wait times or adding/removing content as needed."
}